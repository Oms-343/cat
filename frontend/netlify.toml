# Netlify: admin UI — proxies /api to Render backend (same-origin for the React app).
[[redirects]]
  from = "/api/*"
  to = "https://cat-taof.onrender.com/api/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
